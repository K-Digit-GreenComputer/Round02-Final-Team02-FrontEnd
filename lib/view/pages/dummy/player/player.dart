import 'package:flutter/material.dart';

class PlayerMovie extends StatelessWidget {
  const PlayerMovie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(),
    );
  }
}
